

const radus = 5;
let circleArea = Math.PI * Math.pow(radus);
let circumference = 2*Math.PI*radus;

console.log(`The area is approximately ${circleArea} square units.`);
console.log(`The circumference is approximately ${circumference} units.`);
